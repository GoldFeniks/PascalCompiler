program test;
var a: Integer;
    b: Integer;
    c: array [1..10] of Integer;

procedure proc(a: Integer);
begin
end;

begin
    if (a < b = proc(a) = c[1]) then
    begin
        a := a < b;
        a := -a;
    end
end.